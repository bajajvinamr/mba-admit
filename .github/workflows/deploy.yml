name: Deploy

# TODO: Configure actual deployment triggers and steps
# This is a placeholder workflow for Vercel (frontend) + Railway (backend) deployment.
#
# Planned setup:
#   - Frontend: Vercel auto-deploys from main via GitHub integration
#   - Backend: Railway auto-deploys from main via GitHub integration
#   - This workflow can be used for manual deploys or additional deploy gates

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        default: "staging"
        type: choice
        options:
          - staging
          - production

jobs:
  deploy-frontend:
    name: Deploy Frontend (Vercel)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      # TODO: Add Vercel deploy step
      # - uses: amondnet/vercel-action@v25
      #   with:
      #     vercel-token: ${{ secrets.VERCEL_TOKEN }}
      #     vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
      #     vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
      #     working-directory: frontend/

      - run: echo "Frontend deploy placeholder — configure Vercel integration"

  deploy-backend:
    name: Deploy Backend (Railway)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Add Railway deploy step
      # - uses: bervProject/railway-deploy@main
      #   with:
      #     railway_token: ${{ secrets.RAILWAY_TOKEN }}
      #     service: backend

      - run: echo "Backend deploy placeholder — configure Railway integration"
